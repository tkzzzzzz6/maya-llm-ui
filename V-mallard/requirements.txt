# 阿里云DashScope SDK - 实时多模态API
dashscope>=1.23.9

# 音频处理
pyaudio>=0.2.11

# 视频处理和计算机视觉
opencv-python>=4.8.0

# 数值计算（opencv-python依赖，但明确列出）
numpy>=1.21.0

# 其他可能需要的依赖
# 如果在某些系统上需要额外的音频支持
# portaudio19-dev  # Linux系统可能需要：sudo apt-get install portaudio19-dev
# 如果在Windows上pyaudio安装有问题，可以使用：
# pip install pipwin
# pipwin install pyaudio
